{
  "name": "evict_beyond_head_clamps",
  "comment": "evict{up_to_seq=100} clamps to head=2 (D20: floor <= head+1), raising floor to 3; a read below the floor -> expired with next_cursor=floor-1=2 (== head, since the clamp cannot manufacture positions past head); a read at head -> would_block probe with next_cursor=head=2 (D20, D9, D14)",
  "node": { "stop_when": "last_reader" },
  "steps": [
    { "in": { "type": "push", "payload": "YQ==" } },
    { "in": { "type": "push", "payload": "Yg==" } },
    { "in": { "type": "evict", "up_to_seq": "100" } },
    { "in": { "type": "read", "log_id": "log-A", "stream_id": "s1", "cursor": { "seq": "0" }, "timeout_ms": "0" } },
    { "in": { "type": "read", "log_id": "log-A", "stream_id": "s1", "cursor": { "seq": "2" }, "timeout_ms": "0" } }
  ]
}
