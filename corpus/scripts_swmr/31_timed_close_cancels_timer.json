{
  "name": "timed_close_cancels_timer",
  "comment": "56-F4: a read with no snapshot yet and timeout_ms=50 holds and emits SetTimer{1,50}; close{} releases it EARLY with closed and must cancel the timer first -- SwmrCancelTimer{1} THEN the closed SwmrReadResponse THEN SwmrProducerStop{closed}",
  "node": { "stop_when": "last_reader" },
  "steps": [
    { "in": { "type": "read", "swmr_id": "swmr-A", "stream_id": "s1", "timeout_ms": "50" } },
    { "in": { "type": "close" } }
  ]
}
