{
  "name": "held_read_timer",
  "comment": "read with timeout_ms=50 on a caught-up atom emits SetTimer{1,50}; timer_expired{1} answers would_block (D14/D16 analogues)",
  "node": { "stop_when": "last_reader" },
  "steps": [
    { "in": { "type": "read", "atom_id": "atom-A", "stream_id": "s1", "timeout_ms": "50" } },
    { "in": { "type": "timer_expired", "token": "1" } }
  ]
}
