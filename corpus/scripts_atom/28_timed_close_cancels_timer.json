{
  "name": "timed_close_cancels_timer",
  "comment": "56-F4: read timeout_ms=50 holds and emits SetTimer{1,50}; close{} releases it EARLY with closed and must cancel the timer first -- AtomCancelTimer{1} THEN the closed AtomReadResponse THEN AtomProducerStop{closed}",
  "node": { "stop_when": "last_reader" },
  "steps": [
    { "in": { "type": "read", "atom_id": "atom-A", "stream_id": "s1", "timeout_ms": "50" } },
    { "in": { "type": "close" } }
  ]
}
